# Inputs — historical, not the plan of record

These files are the AI-generated inputs the project started from. They are kept for
provenance only and are **not** normative:

- `PROJECT_PLAN.md`
- `PHASE0_PREREQUISITES.md`
- `PHASE0_ACQUISITION_PLAN.md`
- `PHASE0_REVIEW.md`
- `idea source.txt`

Their algorithmic, gate and citation claims were corrected in the archived OpenSpec
changes (`openspec/changes/archive/`) and are superseded by `openspec/specs/` and
`results/decision_report.md`. Do not derive thresholds, rank formulas, shapes or
method choices from them; several numbers in them are wrong (for example the
`[4096, 11008]` layer shape, the `k = μ·min(m, G)` rank, Shannon-entropy BPW, and the
attribution of TWLA to 2024).

Precedence order for the current sources of truth is in `AGENTS.md`.
