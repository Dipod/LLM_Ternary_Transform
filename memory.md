# memory.md

Strict long-term project memory: facts and decisions that must survive session boundaries and that no other store holds. Entries are dated. No secrets, no PII, no dataset or model excerpts.

Agents add entries when the user asks to remember something, and use this file as the fallback store when no other memory provider can save.

## Entries

- 2026-09-24 — Репозиторий развёрнут как агентный каркас под Python/ML-исследование (тернарная конвертация LLM, TCD / ExTernD-Lite). Правила: `AGENTS.md` плюс шесть файлов в `.kilo/rules/` (verification-gates, research-discipline, python-standards, sdd-integrations, subagents, project-memory). Агенты: 13 ролей с префиксом `py-` в `.kilo/agents/`. Цикл разработки: OpenSpec (`openspec/`, команды `/opsx:propose|apply|archive|explore`), CLI проверен (`OpenSpec root: ok`). MCP-серверы не подключены: факты подтверждаются запуском кода или официальной документацией. Инструменты качества объявлены в правилах (ruff, mypy, pytest, GitHub Actions CI), их конфиги появятся с первым изменением кода. Коммуникационный режим хранится в `.dev.env` (`CAVEMAN=auto`). Следующий шаг — первое изменение OpenSpec: окружение и каркас Phase 0 по критическому пути C2 → C6 → F2.
- 2026-09-24 — Окружение: создан `.venv` на единственном интерпретаторе Python 3.14.3; установлены ruff 0.16.8, mypy 2.3.1, pytest 9.1.1 (запинены в `requirements-dev.txt`). ML-стек (`requirements.txt`: torch, transformers, datasets, numpy, scikit-learn, matplotlib) намеренно не установлен — тяжёлые зависимости ставятся под задачу. Добавлены `pyproject.toml` (конфиги ruff/mypy/pytest), `.github/workflows/ci.yml`, `tests/test_environment.py`. Markdown исключён из ruff: ruff 0.16 переформатирует code-блоки, а `PROJECT_PLAN.md` и `PHASE0_*.md` — human-owned. MCP-серверы по решению пользователя не подключаем.
- 2026-09-24 — Железо: две GPU — NVIDIA T400 4 GB и AMD Radeon RX 7900 XTX 24 GB; пользователь указал использовать AMD 24 GB. Выбор build torch (CPU / CUDA / ROCm) намеренно не зафиксирован, решается при первой задаче, которой torch нужен. Факт ROCm/Windows не проверялся.
- 2026-09-24 — Глобальный `kilo.jsonc` содержал 13 агентов `1c-*` из sibling-проекта «personal cabinet». В проектный `.kilo/kilo.json` добавлен PowerShell-allowlist для bash: глобальный блок разрешений настроен на Unix-команды и не срабатывал. MCP-серверы в этом проекте отсутствуют.
- 2026-09-24 — По единичному разрешению пользователя глобальный конфиг `C:\Users\dipod\.config\kilo\kilo.jsonc` изменён: из секции `agent` удалены 13 агентов `1c-*`. Обоснование: они уже определены в scope соседнего репозитория (`personal cabinet\.kilo\agents\*.md`, 13 tracked-файлов, frontmatter `name: 1c-*`, `mode: subagent`); 12 промптов идентичны глобальным, `1c-tester` локально новее (добавлен блок YaxUnit), т.е. локальные копии — источник истины. Non-1c ключи (`permission`, `provider`, `model`, `indexing`, `experimental`) не тронуты; MCP-серверы 1С остаются в `personal cabinet\.kilo\kilo.json`. Бэкап оригинала: `C:\Users\dipod\AppData\Local\Temp\kilo\kilo.jsonc.bak`. Проверено после перезапуска клиента: конфиг записан 17:51:14Z, рестарт в 17:53:07Z, стартовый лог без ошибок конфигурации; вызов субагента `1c-explorer` по имени в этом проекте даёт «not a valid agent type» — агенты 1С здесь больше не загружаются. Каталог `task` для config-map агентов не показателен (он строится из файловых `.kilo/agents/*.md`), решающей была проверка вызовом по имени.
- 2026-09-24 — Окружение (Kilo Code): активный вопрос к пользователю (question tool) автоматически закрывается при любом сигнале в сессии — завершение фонового субагента, срабатывание таймера и т.п. Такое «dismissed» означает прерывание сигналом, а не отказ пользователя, и не является его ответом.
- 2026-09-24 — Ресурсы машины (инвентарь): CPU AMD Ryzen 5 3500X (6c/6t, 3.6 GHz); RAM 64 GB (≈36 GB свободно); Windows 11 Pro build 26200; диск C: 1407 GB, свободно ≈84 GB. GPU: AMD Radeon RX 7900 XTX 24 GB (OpenCL сообщает 25 753 026 560 байт; Vulkan device присутствует) + NVIDIA T400 4 GB. WSL2: Ubuntu (остановлена) и docker-desktop. Установлены vulkaninfo/clinfo/nvidia-smi.
- 2026-09-24 — Верифицированные факты ускорения: PyTorch ROCm на Windows официально поддержан для gfx1100 (RX 7900 XTX), но требует Python 3.12 и конкретного драйвера Adrenalin; WSL2-путь — Ubuntu 22.04/24.04. Собственный Vulkan-backend PyTorch снят с поддержки (перенесён в ExecuTorch для мобильных GPU); сторонний `torchvulkan` — экспериментальный (0.1.0a1). llama.cpp имеет AMD-валидированные Windows-бинарники (ROCm 7.2.1, gfx110X) и Vulkan-путь, который на gfx1100 часто быстрее ROCm. Вывод: Vulkan — путь инференса (Phase 4/5), не для численных экспериментов на PyTorch.
- 2026-09-24 — Решения Phase 0 (пользователь делегировал): кодбук — строго тернарные B,C ∈ {−1,0,1} + скаляр D (faithful ExTernD); градиенты строго запрещены (closed-form/ALS, важность из E[x²]); единица гейта — один Linear-слой (gate_proj), метрика E_x, perplexity отложена; модель Phase 0 — Qwen2.5-0.5B (hidden 896, inter 4864, 24 слоя, ungated); compute — CPU PyTorch, AMD-ускорение отдельной задачей верификации.
- 2026-09-24 — Backend Phase 0 (изменение пользователя): пользователь дважды выбрал считать calibration-forward и sweep на AMD RX 7900 XTX через ROCm, отклонив CPU-вариант и NVIDIA T400. Выбран путь C-WSL: ROCDXG (production, Adrenalin ≥ 26.2.2 + ROCm 7.2.1) внутри WSL2, отдельный дистр Ubuntu-24.04 (WSL-матрица ROCDXG поддерживает 24.04/26.04; ROCm apt-репозиторий для 26.04 отсутствует, поэтому 24.04). Установленный Windows-драйвер `32.0.31041.1004` = Adrenalin 26.8.1 (таблица GPUOpen) — предусловие драйвера выполнено. PyPI Windows-ROCm колёс нет; нативные Windows-колёса AMD требуют Python 3.12. В WSL ставим ROCm-userspace + prebuilt `rocdxg-roct` deb + torch ROCm-колесо (Ubuntu 24.04 → Python 3.12). Это отдельный environment-change: design.md D9 (CPU) и D11 придётся обновить, CUDA/HIP-ядра не бит-детерминированы — источник недетерминизма обязан быть в report.md. Старый прогон sweep на CPU не записан (процесс убит сменой группы сессии до записи results/).
- 2026-09-24 — ROCm-on-WSL окружение (рабочий рецепт, проверено). Дистр `Ubuntu-24.04` в WSL2 (root, Python 3.12.3). DNS WSL был сломан (`/etc/resolv.conf` отсутствовал): создан с `8.8.8.8/1.1.1.1/9.9.9.9`, зафиксирован `chattr +i`, в `/etc/wsl.conf` — `[network] generateResolvConf = false`. ROCm apt-репозиторий `https://repo.radeon.com/rocm/apt/7.2 noble main` + pin `Pin: release o=repo.radeon.com / Pin-Priority: 600` (иначе apt берёт системный `rocminfo 5.7.1` вместо ROCm 1.0.0.70200). Пакеты: **полный метапакет `rocm`** (без него torch ROCm падает на `libroctx64.so.4`, `rocblas`, `MIOpen`, `hipblas`, `rccl` — минимального `rocm-hip-runtime` недостаточно) + prebuilt `rocdxg-roct_1.2.2_amd64.deb` из GitHub ROCm/librocdxg. venv `/opt/phase0-rocm` (python3.12) с AMD-колесом `torch-2.9.1+rocm7.2.1.lw.gitff65f5bc-cp312-cp312-linux_x86_64.whl` и AMD-`triton-3.5.1+rocm7.2.1...` с `repo.radeon.com/rocm/manylinux/rocm-rel-7.2.1` (не с PyPI: pip не находит `+rocm7.2.1`-версии; колесо качать под правильным именем — `wget -O torch.whl` даёт «Invalid wheel filename»). Проверка: `rocminfo` → gfx1100 / Radeon RX 7900 XTX; `HSA_ENABLE_DXG_DETECTION=1` обязателен (ROCm 7.2 < 7.13). Запуск из `/mnt/c`: `HF_HOME=/mnt/c/LLM/Models`. Кэш HF остаётся на Windows-диске. Известное ограничение vLLM на WSL: pinned memory/UVA недоступен, V2-раннер падает (`UVA is not available`), лечится апстрим-фиксом; llama.cpp/Vulkan — альтернатива инференса.
- 2026-09-24 — Итог Phase 0 (изменение `phase0-ternary-feasibility`): гейт прогнан на AMD RX 7900 XTX (ROCm 7.2.1, torch 2.9.1+rocm7.2.1), device `cuda:0`. Вердикт **YELLOW** (не GREEN): лучший конфиг μ=3, τ=0.7, importance off → `E_x=0.0716` (порог GREEN ≤0.05 не достигнут), sparsity=0.425, `bpw_eff=5.595`, runtime 1.4 c (бюджет 600 c). μ=3 с importance on и все μ=2 — RED; baseline μ=1 — RED (`E_x=0.303`). Калибровка: WikiText-2 train, 128 документов, **11 426 не-pad токенов** (из «до 65 536»), эмбеддинги слоя 12. Валидность метрики подтверждена (perfect `E_x=0`, destroyed `E_x=32.17`). Воспроизводимость: повтор `--verify` совпал при `rtol=1e-5`. Записи — `results/<id>/{config.yaml,metrics.json,report.md}`, `config.yaml` содержит swept-значения. Decomposition (AMD vs Intel CPU) не перепроверялся на CPU. Вывод: факторизованная тернарная декомпозиция на одном слое — YELLOW, GREEN не подтверждён; эскалация (AGA, больше слоёв/модель) — отдельным решением.
- 2026-09-24 — Phase 0 probe (вариант A, пост-гейт, `results/explore_mu3/`): при μ=3 дешёвые ручки **не закрывают** разрыв до GREEN. `n_inner` 15→50 снижает `E_x` 0.0716→**0.0650** (плато, +9%); τ=1.0 → 0.0675 при sparsity 0.58; τ=1.3 → sparsity 0.70, но `E_x` 0.0914; τ=0.5 → sparsity 0.294 (<0.40, GREEN-невалиден) и 0.0757; reorder даёт ≤0.002. Лучшее — 0.0650 (μ3/τ0.7/importance off/reorder off/`n_inner=50`), всё ещё YELLOW. Для probe добавлено измерение `sweep.n_inner` (config schema) и конфиг `phase0_feasibility/config_explore_mu3.yaml`; `results_dir: results/explore_mu3`, архивные записи не тронуты. Вывод: подбором настроек потолок ≈0.065; для GREEN нужен структурный шаг (AGA/учёт важности) либо пересмотр измерения. Решение пользователя (2026-09-24): **сначала проверить измерение** — плотный/больший калибровочный срез и 2–3 слоя, затем решать про метод.
- 2026-09-24 — Re-check калибровки (change `phase0-calibration-robustness`, `results/recheck/`, 54 прогона на ROCm: 2 среза × 3 слоя × 8 конфигов + baseline). **Измерение честное, YELLOW подтверждён.** `E_x_fit ≈ E_x_heldout` везде (расхождение ≤0.005) — переобучения на срез нет. Смена среза не влияет: `dense_windows` (65 536 токенов) vs `many_documents` (1024 док) дают почти одинаковый `E_x` (напр. l12/μ3/τ0.7/imp off: 0.0709 vs 0.0715; лучшие 0.0621 vs 0.0676). Лучшее — `dense_windows`, слой 21, μ3, τ0.7, importance off: `E_x_fit=0.0621` (YELLOW, порог GREEN 0.05). Слой 21 чуть лучше 12 и 3 при μ3/τ0.7, но для `many_documents` слой 3 заметно хуже (0.0898). importance on стабильно ухудшает; μ=2 и baseline μ=1 — RED. **Вывод: потолок ~0.062 — свойство метода (weight-space fit), а не артефакт измерения**; для GREEN нужен структурный шаг (AGA / активационно-взвешенная цель) либо смена гейта. Решение — за пользователем. Также: схема конфига изменена (`target_layer`→`layers`, `n_documents`→`slices`+`dense_windows_count`/`many_documents_count`+`eval_fraction`); `config_explore_mu3.yaml` удалён как несовместимый со старой схемой (записи probe сохраняются в `results/explore_mu3/`); старые `results/<id>` не пере-верифицируемы новым `--verify` (иная схема), но остаются валидной записью.
- 2026-09-24 — μ-curve probe (`results/mu_probe/`, `config_mu_probe.yaml`, 26 прогонов; dense_windows, слои 12/21, τ 0.7/1.0, `n_inner=50`, importance off). Кривая: `E_x_fit` по μ (l21, τ0.7): μ1 0.286, μ2 0.13, μ3 0.064, μ4 **0.031**, μ6 0.007, μ8 0.0016. `bpw_eff` по μ (τ0.7): 1.9 / 3.8 / 5.6 / 7.5 / 11.2 / 14.9. **Вывод: «больше тритов» (в нашей схеме — больше μ, т.е. k=μ·min) резко улучшает точность; цель `E_x≤0.05` достигается уже при μ=4 (0.031), но `bpw=7.5 > 6.0` → GREEN невозможен; μ≥6 (bpw≥10) — RED.** Прежний вывод «выигрыша нет» подтверждён, но **по бюджету сжатия, а не по точности**: accuracy-цель методом достигается, ограничение — правило подсчёта bpw. GREEN-путь: при μ=4 поднять sparsity так, чтобы `bpw<6` (нужно `sparsity > 0.7333` при μ=4), используя запас по E_x. Проверяется τ-probe (`results/tau_probe/`, μ∈{3,4,5} × τ∈{1.0,1.3,1.5,1.7}).
- 2026-09-24 — τ-probe при μ=4 (`results/tau_probe/`, `config_tau_probe.yaml`, 26 прогонов; dense_windows, слои 12/21, τ∈{1.0,1.3,1.5,1.7}, `n_inner=50`, importance off). **GREEN не найден, окно пустое с промахом ~3%.** Фронтир (слой 21): μ4/τ1.3 → `E_x=0.0463` ✓, sparsity 0.699, `bpw=6.165` ✗ (нужно <6.0, т.е. sparsity>0.7333); μ4/τ1.5 → `bpw=5.865` ✓, sparsity 0.762, `E_x=0.0647` ✗; μ4/τ1.7 → `E_x=0.090` ✗; μ5/τ1.3 → `E_x=0.0244` ✓ но `bpw=7.70` ✗ (при μ=5 `bpw<6` требует sparsity>0.9867 — недостижимо); μ3 — `E_x` не ниже 0.062. Т.е. `E_x` пересекает 0.05 (~τ≈1.35) раньше, чем `bpw` пересекает 6.0 (~τ≈1.42). Пути к GREEN: (а) поднять точность при μ=3 (уложиться в `bpw=5.0–5.6`<6) — AGA/активационно-взвешенная цель; (б) при μ=4 снизить `bpw` на ~3% без потери точности (тернаризация, дающая больше нулей при том же E_x: pruned threshold / per-column / двухстадийная). Смена самого правила подсчёта bpw = смена гейта, отдельное решение. Затрагивает вопрос пользователя «нужно ли вообще гнаться за сжатием»: `bpw` — единственный рычаг скорости на decode (bandwidth-bound); RAM vs VRAM ≈10–15× по ПСП, тернарный формат даёт ~2.9× меньше байт → в RAM модель медленнее, выигрыш там в ёмкости, не в скорости.
- 2026-09-24 — Замер железа/посылки (`bench_hw.py`, WSL/ROCm, torch 2.9.1+rocm7.2.1, 6 CPU-потоков, эта машина). **ПСП:** RAM numpy-copy **34.6 ГБ/с** (torch CPU copy 22.2), VRAM (ROCm copy) **744 ГБ/с** → RAM в **~21–33×** медленнее VRAM. **Compute (GEMM слоя [4864,896]):** CPU fp32 **0.124 TFLOPS**; GPU fp16 **47.2**, fp32 **20.3 TFLOPS** → GPU быстрее в ~380× (fp16) / ~164× (fp32). CPU **int8** matmul в этой сборке torch не реализован (`normal_kernel_cpu not implemented for 'Char'`) → без кастомного кернела (Phase 4) готового трит-пути нет. **Вывод: «серьёзная модель в RAM без потери скорости» не подтверждается.** Тернарный формат даёт ~2.9× меньше байт, но RAM ~21–33× медленнее VRAM → decode в RAM ориентировочно в **7–12× медленнее** fp16 в VRAM; выигрыш RAM — **ёмкость/деньги, не скорость**. Против FP32 на том же CPU тернарный RAM-вариант ~5–7× быстрее (оценка только по ПСП; KV-кэш, attention и накладные не учтены). Практический вывод: `bpw` остаётся главным рычагом скорости генерации; если цель меняется на ёмкость, порог `bpw<6.0` нужно переопределять (смена постановки — отдельное решение).
- 2026-09-24 — AGA-прототип (ad-hoc `aga_proto.py`; dense_windows, fit 102 / held-out 26 окон, слои 12 и 21, μ∈{3,4} × τ∈{0.7,1.0,1.3,1.5}, `n_inner=50`). AGA = пересчёт **только** `D` по выходной метрике (закрытая форма `A D = b`, `A=(C M Cᵀ)⊙(BᵀB)`, `b=diag(C M Wᵀ B)`, `M=X_fitᵀX_fit`); sparsity и `bpw` при этом не меняются. **Механизм подтверждён и переносится на held-out (fit и ho движутся вместе), но эффект мал — ~3–4% относительного.** Примеры (слой 21): μ3/τ0.7 fit 0.0579→0.0562, ho 0.0578→0.0562; μ4/τ1.3 fit 0.0461→**0.0449**, ho 0.0460→0.0451; μ4/τ1.5 fit 0.0648→0.0634, ho 0.0648→0.0636. Лучшее после AGA: **μ4/τ1.3 → E_x≈0.045–0.046, sparsity 0.699, bpw 6.156–6.165** — точность с запасом, но `bpw` на **2.6%** выше порога 6.0. **Вывод: AGA сама по себе GREEN не даёт** (нужно ~20% улучшения, дала ~3.5%); блокирует исключительно правило подсчёта `bpw`. При информационной оценке бит (≈log2(3)/энтропия) тот же конфиг дал бы ≈5.6 bpw → GREEN. Значит развилка сместилась с метода на **правило учёта бит** (смена гейта — решение пользователя, спекой прямо запрещена энтропийная постановка). Источники: PT²-LLM `arXiv:2510.03267` (ITF+AGA), TWLA `arXiv:2606.13054` (Stage II через `S=ΣXᵀX`).
- 2026-09-24 — Prune+AGA прототип (`aga_prune_proto.py`): идея — подрезать слабейшие по `|D|` компоненты (каждый убирает столбец B и строку C), поднять sparsity и сбить `bpw`, затем пересчитать `D` (AGA). **Провал:** подрезка слабых компонент **снижает** sparsity (слабые компоненты сами разрежены — их нули уходят вместе с ними), `bpw` почти не падает, `E_x` взрывается (при k'→1 `E_x`→0.99); порог sparsity 0.7333 не достигается (цикл подрезки срывается до k'=1/827). Итог: подрезка как рычаг `bpw` не работает. **Сводный фронтир при μ=4 (после AGA):** τ=1.3 → `E_x` 0.045–0.046 ✓, sp 0.699, bpw 6.16 ✗; τ=1.4 → sp 0.733, bpw 5.37 ✓, `E_x` ~0.069 ✗; τ=1.5 → bpw 5.86 ✓, `E_x` ~0.063 ✗. **В этом семействе метода (глобальный `k=μ·min`, `T_τ`, weight-space fit + AGA-пересчёт `D`) GREEN при фиксированном гейте недостижим; `E_x` пересекает 0.05 раньше, чем `bpw` опускается ниже 6.0.** Блокирует правило подсчёта бит. Остающиеся пути: (C1) пересмотр правила учёта `bpw` (смена гейта — решение пользователя); (A2) структурно другой метод (метрико-осознанная оптимизация самого тернарного паттерна, GPTQ-подобная компенсация, асимметричный сдвиг строк, per-row τ); (D) принять YELLOW.
- 2026-09-24 — **Ресёрч посылки проекта (три независимых агента: ландшафт точности, железо, go/no-go; веб-источники 2025–2026). Вердикт: текущая постановка «тернарный PTQ как конкурент 4-бит» не имеет подтверждения; исходная метафора выигрыша («умножение→сложение») для нашего стенда неверна.**
  - **Точность:** 4-бит (GPTQ/AWQ g128, независимо: LLaMA3-70B 3.3 vs 2.9 PPL; Qwen3-14B MMLU −1%) near-lossless на ≥7B. Тернарный PTQ теряет 5–11 пунктов на 7–9-задачных средних (PT²-LLM LLaMA-7B −5.8; CAT-Q Qwen3-8B −9.8) и катастрофически на reasoning (Math-500 Qwen3-8B 96.8→58.4). **Бит-эффективность тоже хуже:** ExTernD (arXiv:2607.13511) выходит на паритет с Q4_K лишь при 5.2–5.7 эфф. bpw против ~4.5–4.9 у Q4_K. Наши 6.16 bpw при достигнутой точности — тот же проигрыш по битам.
  - **Железо:** batch-1 decode bandwidth-bound (AI 1–10 FLOP/byte; prefill 55–100), поэтому удаление умножения не трогает узкое место; реальный рычаг — байты (`bpw`). На datacenter-GPU **нет нативного тернарного tensor core**, int4/fp4 есть (NVFP4 ~2.35× над INT4 W4A16). Нули тернарных (42% у BitNet) **неструктурированы и не пропускаются** боевыми кернелами; измеренный тернарный выигрыш над int4 на batch-1 всего 1.22–1.37× (llama.cpp TQ2_0 vs Q4_0).
  - **Compute инвертирован:** наша факторизация делает `k(m+n)` тернарных сложений против `mn` FMA → `μ(m+n)/max ≈ 3.5–4.7×` больше операций при μ=3–4; на GPU add≈FMA ⇒ медленнее dense fp16. Выигрыш только там, где умножение дорого (FPGA/ASIC/CPU masked-add).
  - **«Без дообучения» — проигрышный режим:** ниже 4 бит PTQ сильно теряет; разрыв закрывают QAT/адаптацией. ParetoQ (NeurIPS'25, независимо): 1.58/2/3-бит сопоставимы и часто лучше 4-бит на фронтире «точность/размер», **но 2-бит — деплойный выбор** (1.58 бита аппаратно неудобен).
  - **Ниши, где тернарные всё же выигрывают:** CPU/edge batch-1 (bitnet.cpp 2.37–6.17× vs FP16 x86, энергия −55..−82%; T-MAC до 4×), экстремальная память (MoE в RAM), без-умножительное FPGA/ASIC-железо (TerEffic 192×/19×, TCN-CUTIE), нативное тернарное обучение (BitNet 2B4T ~паритет fp16 на малом размере).
  - **Риск источников:** ExTernD — одиночный нерецензируемый preprint, на нём держится Phase 0; все шесть тернарных методов — author-reported; независимого head-to-head «тернарные vs 4-бит» не найдено. PHASE0_REVIEW содержит ошибки (TWLA — ICML 2026, а не 2024; у PTQTP две трит-плоскости ~3.16 bpw, не K=4).
  - **Гейт проверяет не ту гипотезу:** он сертифицирует восстановление одного слоя против FP16, а не конкурентоспособность против 4-бит; 0.5B ниже пола цитируемых PTQ-работ (≥1.7B), где тернарные теряют меньше.
  - **Самый дешёвый решающий эксперимент (рекомендация агента 3):** фронтир «точность ↔ эффективные биты» против 4-бит baseline (`AWQ`/`GPTQ` g128 или `Q4_K_M`) на той же модели/срезе/seed — часы на одной 24 ГБ GPU; тернарная сторона почти готова, 4-битная — одна команда. Предрегистрированный критерий: CONTINUE, если тернарная кривая не правее 4-бит при целевой точности; PIVOT на 2-бит, если проигрыш ≥1% PPL; STOP, если при ≤1.6 bpw тернарные теряют >2% относит. PPL против 4-бит. Решение — за пользователем.
- 2026-09-24 — **ПРЕДРЕГИСТРАЦИЯ решающего эксперимента (зафиксировано ДО прогона; пользователь выбрал вариант A).** Сравнение на одном слое, той же модели (Qwen2.5-0.5B), том же срезе (dense_windows, fit/held-out 80/20), том же seed и тех же активациях: (a) тернарная сторона — лучшие измеренные точки (μ=4/τ=1.3: `E_x=0.045`, sp 0.699, `bpw=6.16`; μ=3/τ=0.7: `E_x≈0.056`, `bpw≈5.62`); (b) 4-битная сторона — symmetric group-128, RTN и GPTQ (Hessian из тех же `X_fit`), учёт `bpw = 4 + 16/128 = 4.125` (fp16 scale на группу 128; при scale+zero было бы 4.25). Метрика — layer-local `E_x` fit и held-out (формула гейта), идентичная для обеих сторон. **Правило решения: STOP, если 4-битная точка строго доминирует лучшую тернарную (меньше `E_x` И меньше `bpw`); CONTINUE только если тернарная точка имеет меньший `E_x`; PIVOT на 2-бит рассматривается отдельно, если 4-бит доминирует, но разрыв по `E_x` мал.** Стенд: AMD RX 7900 XTX / ROCm, WSL venv. Если 4-бит доминирует — фиксируем «не подтверждено» и закрываем постановку «тернарный PTQ против 4-бит».
- 2026-09-24 — **РЕЗУЛЬТАТ решающего эксперимента: STOP (тернарная факторизация доминируется обычным целочисленным групповым квантованием).** Доказательства: `results/bit_frontier/{prototype.py,prototype_clip.py,report.md}`. Модель/срез/метрика — как предрегистрировано; sanity 8-бит `E_x=0.0045` (пайплайн верен). **При равных битах** (layer-local `E_x`, слой 12 / слой 21): 4-бит g128 (bpw 4.125) → 0.083/0.070; 5-бит g128 (bpw 5.125) → 0.039/0.035; **6-бит g128 (bpw 6.125) → 0.019/0.018**; тернарные μ=4/τ=1.3 (bpw 6.160) → **0.048/0.046**. То есть при ~том же бюджете бит целочисленное 6-бит даёт `E_x` в **~2.5× меньше**, а 5-бит (на 17% меньше бит) тоже лучше тернарных. Тернарные точки при bpw 4.4–4.6 (0.088–0.115) проигрывают и 4-бит (0.070–0.085) при 4.125. **Правило даёт STOP: целочисленный фронтир лежит ниже тернарного на всём диапазоне; целочисленный формат зрелый, аппаратно ускорен и не требует факторизации.** Оговорки: layer-local `E_x`, одна модель/срез, целочисленное плечо — рукописные RTN/GPTQ с clipping (sanity пройден; библиотечный AWQ/GPTQ мог бы быть только лучше), end-to-end PPL не измерялась. Вывод соответствует ресёрчу посылки (три агента): как «сжатие против 4-бит» постановка не подтверждена; ниша тернарных — CPU/edge/FPGA и нативное обучение, а не GPU-квантование.
