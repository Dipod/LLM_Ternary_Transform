# memory.md

Strict long-term project memory: facts and decisions that must survive session boundaries and that no other store holds. Entries are dated. No secrets, no PII, no dataset or model excerpts.

Agents add entries when the user asks to remember something, and use this file as the fallback store when no other memory provider can save.

Consolidated 2026-09-25: superseded and one-off entries were removed; the durable history of Phase 0 now lives in `openspec/changes/archive/`, `openspec/specs/` and `results/decision_report.md`.

## Entries

- 2026-09-24 — MCP-серверы в этом проекте **не подключены** (решение пользователя): любой факт о библиотеке, форме тензора, метрике или поведении модели подтверждается запуском минимального сниппета или официальной документацией, не памятью. Коммуникационный режим — `CAVEMAN=auto` в `.dev.env`.
- 2026-09-24 — Окружение и пины: `.venv` на Python 3.14.3; `requirements.txt` запинен (torch 2.14.0, transformers 5.17.0, datasets 5.0.1, numpy 2.5.3, scikit-learn 1.9.1, matplotlib 3.11.2, pyyaml 6.0.3), `requirements-dev.txt` — ruff 0.16.8 / mypy 2.3.1 / pytest 9.1.1. Инструменты (ruff, mypy) нацелены на **Python 3.12**, потому что `numpy 2.5.3` требует ≥3.12 (`requires-python`). Markdown исключён из ruff (он переформатирует code-блоки, а `inputs/*.md` human-owned). CI ставит runtime+dev зависимости; network-тесты помечены `@pytest.mark.network` и исключены из дефолтного прогона.
- 2026-09-24 — Машина и локальные правки конфигов. Железо: CPU AMD Ryzen 5 3500X (6c/6t), RAM 64 GB, Windows 11 Pro build 26200; GPU — AMD Radeon RX 7900 XTX 24 GB (compute) и NVIDIA T400 4 GB (только дисплей, не для вычислений). WSL2: дистрибутивы Ubuntu-24.04 (стенд ROCm) и Ubuntu 26.04. Правки конфигов: в проектный `.kilo/kilo.json` добавлен PowerShell-allowlist для bash (глобальный блок настроен на Unix-команды); по разрешению пользователя из глобального `C:\Users\dipod\.config\kilo\kilo.jsonc` удалены 13 агентов `1c-*` (они определены в sibling-репозитории «personal cabinet», который там — источник истины), проверено вызовом по имени после рестарта; бэкап `%TEMP%\kilo\kilo.jsonc.bak`.
- 2026-09-24 — ROCm-on-WSL окружение (рабочий рецепт, проверено). Дистр `Ubuntu-24.04` в WSL2 (root, Python 3.12.3). DNS WSL был сломан (`/etc/resolv.conf` отсутствовал): создан с `8.8.8.8/1.1.1.1/9.9.9.9`, зафиксирован `chattr +i`, в `/etc/wsl.conf` — `[network] generateResolvConf = false`. ROCm apt-репозиторий `https://repo.radeon.com/rocm/apt/7.2 noble main` + pin `Pin: release o=repo.radeon.com / Pin-Priority: 600` (иначе apt берёт системный `rocminfo 5.7.1` вместо ROCm 1.0.0.70200). Пакеты: **полный метапакет `rocm`** (минимального `rocm-hip-runtime` недостаточно: torch ROCm падает на `libroctx64.so.4`, `rocblas`, `MIOpen`, `hipblas`, `rccl`) + prebuilt `rocdxg-roct_1.2.2_amd64.deb` из GitHub ROCm/librocdxg. venv `/opt/phase0-rocm` (python3.12) с AMD-колесом `torch-2.9.1+rocm7.2.1...cp312...whl` и AMD-`triton-3.5.1+rocm7.2.1...` с `repo.radeon.com/rocm/manylinux/rocm-rel-7.2.1` (не с PyPI; качать под правильным именем — `wget -O torch.whl` даёт «Invalid wheel filename»). Проверка: `rocminfo` → gfx1100 / Radeon RX 7900 XTX; `HSA_ENABLE_DXG_DETECTION=1` обязателен (ROCm 7.2 < 7.13). Запуск из `/mnt/c`: `HF_HOME=/mnt/c/LLM/Models`. Windows-драйвер `32.0.31041.1004` = Adrenalin 26.8.1 (≥ требуемого 26.2.2). Известное ограничение vLLM на WSL: pinned memory/UVA недоступен, V2-раннер падает (`UVA is not available`); llama.cpp/Vulkan — альтернатива инференса.
- 2026-09-24 — Замеры железа этой машины (WSL/ROCm, torch 2.9.1+rocm7.2.1, 6 CPU-потоков): ПСП RAM numpy-copy **34.6 ГБ/с** (torch CPU 22.2), VRAM **744 ГБ/с** (RAM в ~21–33× медленнее); CPU fp32 GEMM **0.124 TFLOPS**, GPU fp16 **47.2**, fp32 **20.3 TFLOPS** (GPU быстрее в ~380×/164×); CPU int8 matmul в этой сборке torch не реализован. **Вывод: «серьёзная модель в RAM без потери скорости» не подтверждается** — тернарный формат даёт ~2.9× меньше байт, но RAM много медленнее VRAM, поэтому decode в RAM ориентировочно в 7–12× медленнее fp16 в VRAM; выигрыш RAM — ёмкость/деньги, не скорость. Против FP32 на том же CPU тернарный RAM-вариант ~5–7× быстрее (оценка только по ПСП). `bpw` — главный рычаг скорости генерации.
- 2026-09-24 — Итог Phase 0 (изменения `phase0-ternary-feasibility`, затем `phase0-calibration-robustness`; оба архивированы, контракты — `openspec/specs/phase0/{ternary-decomposition,feasibility-gate,calibration-protocol}`). Гейт на AMD RX 7900 XTX/ROCm: лучший результат **YELLOW** — μ=3, τ=0.7, importance off → `E_x=0.0716`, sparsity 0.425, `bpw_eff=5.595`; baseline μ=1 RED (`E_x=0.303`). Пробы: подбор настроек даёт плато ~0.065; смена среза не влияет (dense_windows 65 536 токенов vs 1024 документа дают почти одинаковый `E_x`; `E_x_fit ≈ E_x_heldout`, расхождение ≤0.005 → измерение честное); μ-кривая: больше тритов → точность (μ=4 → `E_x=0.031`) но `bpw≈7.5`; τ-проба: **окно GREEN пустое** (`E_x` пересекает 0.05 раньше, чем `bpw` опускается ниже 6.0); AGA (пересчёт `D` по выходной метрике, закрытая форма через `XᵀX`) работает и переносится на held-out, но даёт лишь ~3.5%; подрезка слабых компонент как рычаг `bpw` провалилась. **Прочная мысль: accuracy-цель методом достигается (μ=4), ограничение — правило подсчёта `bpw`; но `bpw`-порог 6.0 — реалистичная аппаратная модель (маска+знак), а не «спортивное» ужесточение.** Записи: `results/{explore_mu3,recheck,mu_probe,tau_probe}/` (git-ignored).
- 2026-09-24 — Ресёрч посылки (три независимых агента: ландшафт точности, железо, go/no-go; веб-источники 2025–2026): **текущая постановка «тернарный PTQ как конкурент 4-бит» не подтверждается.** (1) Точность: 4-бит g128 near-lossless на ≥7B (независимо: LLaMA3-70B 3.3 vs 2.9 PPL; Qwen3-14B MMLU −1%), тернарный PTQ теряет 5–11 пунктов и обваливается на reasoning (Math-500 96.8→58.4); бит-эффективность тоже хуже (ExTernD выходит на паритет с `Q4_K` лишь при 5.2–5.7 bpw против ~4.5–4.9). (2) Железо: batch-1 decode bandwidth-bound, поэтому удаление умножения не трогает узкое место; на datacenter-GPU нет нативного тернарного tensor core (int4/fp4 есть); нули тернарных (~42%) неструктурированы и не пропускаются; факторизация делает `μ(m+n)/max ≈ 3.5–4.7×` больше операций, чем dense-слой. (3) «Без дообучения» — проигрышный режим; разрыв закрывают QAT/адаптацией (ParetoQ, NeurIPS'25: 2-бит — деплойный выбор). Ниши тернарных: CPU/edge batch-1 (`bitnet.cpp` 2.37–6.17× vs FP16, энергия −55..−82%), FPGA/ASIC без умножений, экстремальная память, нативное обучение. Источники: PT²-LLM `arXiv:2510.03267`, TWLA `arXiv:2606.13054`, ExTernD `arXiv:2607.13511` (одиночный нерецензируемый preprint — на нём держался Phase 0), ParetoQ `arXiv:2502.02631`.
- 2026-09-24/25 — **Решение STOP (предрегистрировано и выполнено).** Правило зафиксировано до прогона: сравнение при **равных битах** (layer-local `E_x`, одна модель/срез/активации) с сильнейшим целочисленным baseline, проверенным sanity-прогоном (8-бит `E_x=0.0045`). Результат (слой 12 / 21): 4-бит g128 (bpw 4.125) → 0.083/0.070; 5-бит (5.125) → 0.039/0.035; **6-бит (6.125) → 0.019/0.018**; тернарные μ=4/τ=1.3 (bpw 6.160) → **0.048/0.046**. Целочисленный фронтир лежит ниже тернарного на всём диапазоне (при равных битах — ~2.5× лучше) → **STOP** постановки «тернарный PTQ против 4-бит». Оговорки: layer-local, одна модель 0.5B, рукописный RTN/GPTQ с clipping (sanity пройден; библиотечный был бы сильнее), end-to-end PPL не измерялась — все в пользу STOP. Решение и доказательства: `results/decision_report.md`, `results/bit_frontier/`; статус в `openspec/project.md`. Возможные пивоты: 2-битный целочисленный PTQ, либо CPU/edge-ниша с тернарным кернелом.
- 2026-09-25 — rule-friction:autonomy.deadline-budget — предложение R-007 («расходовать выданное off-peak-окно до дедлайна, не останавливаться на первой контрольной точке») отклонено пользователем на /evolve; повторно из тех же эпизодов не предлагать, 2026-09-25.

- 2026-09-25 — **Phase 1 Stage A (`phase1-redundancy-cartography`, пакет `phase1_compression/`): VOID, вердикт не выдан.** Спека R4 требует 8-бит sanity ≤ +0.1%; получено **+0.167%** (clip-поиск) и **+0.153%** (чистый absmax — значит clip не виноват) → харнесс объявлен невалидным, запись `results/p1_verdict/`. Карта `results/p1_map_2026-09-25e/`: **слои не удаляются** (лучший одиночный KL 0.1105 > бюджета 0.10), **головы не удаляются** (5% → KL 2.8884), **~5% FFN-размерностей** в бюджете (5837/116736 → KL 0.0903; 10% → 0.2074), **~32 наименее значимых residual-направления** на полосу (KL 0.081 при 32, 0.113 при 64). Тернарная ось (TCD, слой 12 gate_proj): natural zero fraction 0.4227/0.4252/0.4285 при μ=2/3/4 (E_x 0.143/0.071/0.036) — мы попадаем ровно в «долину 42%» Sparse-BitNet, **но пост-хок она не расширяется**: N:M по факторам катастрофичен (2:4 → E_x 0.5617, 4:8 → 0.5263, 6:8 → 0.2235), дроп 20% плоскостей → 0.0623 против 0.0359. End-to-end на `Qwen/Qwen3-8B` (WikiText-2 test, 128×512 = 65 536 токенов; reference PPL 12.6398 при 16 381 470 720 байт; peak VRAM 15 624 МБ): удаление 5% FFN (22 119 размерностей, 271.8M параметров) → PPL 12.5867 (**−0.42%**) при 1.0343×; 6% → −0.36% при 1.0415×; **8% → +5.24%** при 1.0561×; **10% → +67.73%** при 1.0711× — обрыв между 6% и 8%; дальше разрушение (15% → PPL 1 026, 25% → PPL 11 361). Целочисленные baseline (g128, RTN+clip): 4-бит 2.698× → +9.70%; 5-бит 2.361× → +2.47%; 6-бит 2.098× → +0.94%; 8-бит 1.716× → +0.167% — **ни один не проходит ±0.5%**. Вывод: пост-хок структурная избыточность Qwen3-8B ≈ 4% параметров (≈1.04×), до 2× не хватает в разы; KL-прокси консервативен на 5% и оптимистичен на 10%; оба порога (±0.5% и 0.1%) строже добросовестного RTN. Открытые вопросы к пользователю: калибровка порогов, пивот на MoE-удаление экспертов (REAP/Fisher-MoE) или тернарный кодек, полный набор бенчмарков. Окружение: в ROCm-venv `/opt/phase0-rocm` добавлены `lm-eval 0.4.13`, `accelerate 1.15.0` и `torchao 0.13.0` с constraints-пинами (torch 2.9.1+rocm7.2.1 и transformers 5.17.0 не тронуты); ROCm-torch принимает memory-stats только в no-arg форме; `torchao` int4 требует AMD CDNA2+ и на gfx1100 (RDNA3) не запускается. **Развязка VOID — дефект порога, не харнесса:** библиотечный 8-бит (`torchao` int8 weight-only, g128) даёт +0.38% — хуже моего RTN (+0.167%), то есть допуск 0.1% ниже 8-битного фронтира этой модели; калибровка порога — отдельное решение. **Направление пост-хок структурного удаления закрыто решением пользователя (2026-09-25)**; выжившие кандидаты Stage B: удаление экспертов MoE, тернарный кодек, проверка масштаба.
